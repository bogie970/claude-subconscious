# Hermes Memory System — minimal dependencies
# Only what's needed for the memory subsystem (not GPU rental, dashboard, etc.)

# Vector store
lancedb>=0.30.2
pylance>=0.30.2
pyarrow>=23.0.1

# Embeddings
sentence-transformers>=5.4.0

# Schema
pydantic>=2.10.0

# Hybrid search
rank-bm25>=0.2.2

# Concurrency / singleton locks
# Both are used: portalocker for write_gate singleton, filelock for whispers/blocks
portalocker>=2.8
filelock>=3.15

# HTTP — Ollama client, embed daemon, healthchecks
requests>=2.31

# HTTP/2 — required by steward_proxy.py so the upstream httpx.AsyncClient(http2=True)
# negotiates h2 to api.anthropic.com (matches Claude Code's transport; MUST-FIX M2).
# Without this, httpx silently falls back to HTTP/1.1 — the one transport tell worth fixing.
h2>=4.1

# MCP server (memory_server.py exposes memory_search / memory_save tools)
mcp>=1.0

# YAML — eval harness configs, migration scripts, plugin tests
pyyaml>=6.0

# Optional LLM SDKs (lazy-imported, only needed if you set the matching API key).
# Uncomment if you want them installed by default:
# anthropic>=0.39
# openai>=1.40
